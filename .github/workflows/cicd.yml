name: Flask App

on:
  push:
      branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
        - uses: actions/checkout@v4
        - name: Set up Python 3.9
          uses: actions/setup-python@v3
          with:
            python-version: 3.9
        - name: List Files
          run: |
            echo "Hello world"